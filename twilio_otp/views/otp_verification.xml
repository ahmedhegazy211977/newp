<odoo>
    <template id="otp_verification">
        <t t-call="website.layout">
            <style>
                .twilio_custom {
                margin: 0;
                height: 100vh;
                padding: 0;
                font-family: sans-serif;
                /* background: linear-gradient(#c538ab, #2760a1); */
                }

                .login-box {
                position: absolute;
                top: 50%;
                left: 50%;
                width: 400px;
                padding: 40px;
                transform: translate(-50%, -50%);
                background: #000;
                box-sizing: border-box;
                box-shadow: 0 15px 25px rgba(0,0,0,.6);
                border-radius: 10px;
                }

                .login-box h2 {
                margin: 0 0 30px;
                padding: 0;
                color: #fff;
                text-align: center;
                }

                .login-box .user-box {
                position: relative;
                }

                .login-box .user-box input {
                width: 100%;
                padding: 10px 0;
                font-size: 16px;
                color: #fff;
                margin-bottom: 30px;
                border: none;
                border-bottom: 1px solid #fff;
                outline: none;
                background: transparent;
                }

                .login-box .user-box label {
                position: absolute;
                top: 0;
                left: 0;
                padding: 10px 0;
                font-size: 16px;
                color: #fff;
                pointer-events: none;
                transition: .5s;
                }

                .login-box .user-box input:focus ~ label,
                .login-box .user-box input:valid ~ label {
                top: -20px;
                left: 0;
                color: #448bf6;
                font-size: 12px;
                }

                .login-box button {
                position: relative;
                display: inline-block;
                padding: 10px 20px;
                color: #26a6b7;
                font-size: 16px;
                text-decoration: none;
                text-transform: uppercase;
                overflow: hidden;
                transition: .5s;
                margin-top: 40px;
                letter-spacing: 4px;
                border: none;
                background: transparent;
                cursor: pointer;
                }

                .login-box button:hover {
                background: #03a8f4;
                color: #fff;
                border-radius: 5px;
                box-shadow: 0 0 5px #0327f4,
                0 0 25px #0b26bd,
                0 0 50px #0343f4,
                0 0 100px #1e58d5;
                }

                .login-box button span {
                position: absolute;
                display: block;
                }

                .login-box button span:nth-child(1) {
                top: 0;
                left: -100%;
                width: 100%;
                height: 2px;
                background: linear-gradient(90deg, transparent, #0388f4);
                animation: btn-anim1 1s linear infinite;
                }

                @keyframes btn-anim1 {
                0% {
                left: -100%;
                }
                50%, 100% {
                left: 100%;
                }
                }

                .login-box button span:nth-child(2) {
                top: -100%;
                right: 0;
                width: 2px;
                height: 100%;
                background: linear-gradient(180deg, transparent, #0394f4);
                animation: btn-anim2 1s linear infinite;
                animation-delay: .25s;
                }

                @keyframes btn-anim2 {
                0% {
                top: -100%;
                }
                50%, 100% {
                top: 100%;
                }
                }

                .login-box button span:nth-child(3) {
                bottom: 0;
                right: -100%;
                width: 100%;
                height: 2px;
                background: linear-gradient(270deg, transparent, #039cf4);
                animation: btn-anim3 1s linear infinite;
                animation-delay: .5s;
                }

                @keyframes btn-anim3 {
                0% {
                right: -100%;
                }
                50%, 100% {
                right: 100%;
                }
                }

                .login-box button span:nth-child(4) {
                bottom: -100%;
                left: 0;
                width: 2px;
                height: 100%;
                background: linear-gradient(360deg, transparent, #036bf4);
                animation: btn-anim4 1s linear infinite;
                animation-delay: .75s;
                }

                @keyframes btn-anim4 {
                0% {
                bottom: -100%;
                }
                50%, 100% {
                bottom: 100%;
                }
                }
            </style>
            <div class="twilio_custom">
                <div class="login-box">
                    <h2>Enter OTP</h2>
                    <form method="post" action="/web/signup/verify">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <div class="user-box">
                            <input type="text" name="otp" required="required"/>
                            <label for="otp">OTP</label>
                        </div>
                        <button type="submit">
                            <span></span>
                            <span></span>
                            <span></span>
                            <span></span>
                            Verify and Sign Up
                        </button>
                        <label id="otp-message" style="color: red; display: none;"></label>
                        <button type="button" id="resend-otp">
                            <span></span>
                            <span></span>
                            <span></span>
                            <span></span>
                            Resend OTP
                        </button>
                    </form>
                </div>
            </div>
            <script>
                document.getElementById('resend-otp').addEventListener('click', function() {
                fetch('/web/otp/resend', {
                method: 'POST',
                headers: {
                'Content-Type': 'application/json'
                },
                body: JSON.stringify({})
                })
                .then(response => response.json())
                .then(data => {
                const messageLabel = document.getElementById('otp-message');
                if (data.success) {
                messageLabel.style.color = 'red';
                messageLabel.textContent = data.error || 'Failed to resend OTP. Please try again.';
                } else {
                messageLabel.style.color = 'green';
                messageLabel.textContent = 'OTP has been resent successfully!';
                }
                messageLabel.style.display = 'block';
                })
                .catch(error => {
                console.error('Error:', error);
                const messageLabel = document.getElementById('otp-message');
                messageLabel.style.color = 'red';
                messageLabel.textContent = 'An error occurred. Please try again later.';
                messageLabel.style.display = 'block';
                });
                });


            </script>
        </t>
    </template>

    <template id="signup_error" name="Signup Error Page">
        <t t-call="website.layout">
            <div class="container mt16 mb16">
                <div class="alert alert-danger">
                    <t t-esc="error"/>
                </div>
            </div>
        </t>
    </template>
</odoo>
